# Cấu hình Nginx cho môi trường UAT (Testing/Staging)
server {
    listen 80;
    server_name uat.pharmacy-project.com; # Thay thế bằng domain UAT thực tế

    # Khai báo DNS nội bộ của Docker Swarm (cập nhật cache sau mỗi 5 giây)
    resolver 127.0.0.11 valid=5s;

    # Thư mục chứa các file static đã build của React
    root /usr/share/nginx/html;
    index index.html;

    # Bảo mật: Giới hạn truy cập bằng HTTP Basic Authentication
    # Lập trình viên cần tạo file .htpasswd trên host và mount vào container
    auth_basic "UAT Testing Environment - Please login";
    auth_basic_user_file /etc/nginx/.htpasswd;

    # Xử lý Routing SPA (React Router)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy ngược (Reverse Proxy) sang Backend Spring Boot
    location /api {
        # Đưa upstream vào biến sử dụng tasks. để Nginx resolve động mỗi 5s và tránh check lúc startup
        set $backend_upstream http://tasks.spring-boot:8081;
        proxy_pass $backend_upstream;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Bỏ Basic Auth đối với API nếu backend có cơ chế xác thực JWT riêng
        auth_basic off;
    }

    # Cache cấu hình cho assets tĩnh
    location ~* \.(?:css|js|jpg|jpeg|gif|png|ico|cur|gz|svg|svgz|mp4|ogg|ogv|webm|htc)$ {
        expires 7d;
        access_log off;
        add_header Cache-Control "public";
        auth_basic off; # Cho phép tải asset tĩnh không cần qua Basic Auth để tăng tốc độ load
    }
}
